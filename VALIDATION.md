# 验证记录与实机复测

## 本次实际完成的检查

- Tweak、Keyboard、SceneLifecycle、Radius 四个模块经过 Logos 预处理；设置页按 Objective-C 直接检查。
- 用 Clang 22.1.7、Theos iPhoneOS16.5 SDK，分别检查 arm64-apple-ios16.0 和 arm64e-apple-ios16.0，共十组。参数包含 -fobjc-arc、-fblocks、-fsyntax-only、-Wall、-Wextra、-Werror=format；最终十组均无错误、无警告。
- 原 scripts/verify-frozen-foundation.sh 通过。
- scripts/verify-lifecycle-repair.ps1 和统一后的根目录入口通过。
- scripts/verify-energy-repair.py 的源码契约检查通过。
- 从最终源码直接提取的通知读取/发布 C 函数，使用 mock notify 编译为 WebAssembly，并在独立 Node 运行时执行：目标隔离、首次读取、不变状态缓存、20,000 次重复触摸读取、10,000 次重复发布，以及 check/get/set/post 失败恢复均通过。
- 打包验证 shell 脚本语法通过；plist/XML、版本字段与最终 ZIP 完整性由交付脚本检查。

Windows 本地没有完成 Theos 链接、合并架构、ldid 签名、DEB 包验证或 iPhone 安装。因此本次交付是源码 ZIP，不是已验证可安装的 DEB。编译语法通过不等于 iOS 私有 API 在设备上可用，也不等于已验证触摸手感和耗电改善。

两个旧验证脚本在原包中已互相矛盾：scripts 下版本拒绝本来就在过滤器内的微信条目；根目录副本仍要求已经过时的 0.18s settle 值。前者已改为核验当前原有过滤器，后者改为转发到 scripts 下的唯一实现。没有为了通过检查而修改手势时间和注入范围。

## 如何构建

现有 .github/workflows/build.yml 已更新为 0.9.58，新增能耗修复回归检查，仍使用项目原来的 Theos/rootless/ldid 构建方式。把修复源码放进你原来的构建项目后手动运行该 workflow，或在已配置好 Theos 的环境中运行：

~~~sh
python3 scripts/verify-energy-repair.py --cc clang
bash scripts/verify-frozen-foundation.sh
make clean package FINALPACKAGE=1 messages=yes
bash scripts/verify-package.sh packages/实际生成的包名.deb
~~~

本次没有向任何仓库上传或触发远程构建。第三方验证用 SDK、Logos、Substrate 头文件不随源码包分发。

## 先验证功能，再比较功耗

| 场景 | 要确认的结果 |
|---|---|
| 冷启动后开小窗 | 画面能加载，无无限遮罩或黑屏 |
| 连续打开/关闭同一应用 20 次 | 无旧键盘残留、无串会话触摸 |
| 快速在两个应用间切换小窗 | 正确目标获得路由，原目标恢复正常 |
| 弹出/收起系统及第三方键盘 | 点击、输入、避让、外部关闭均正常 |
| 进入挂靠、快速拖动、手指停住再继续 | 动画保持跟手；停住时没有持续空回调 |
| 快速松手 / 取消手势 | 最后位置参与落点计算，不跳回旧位置 |
| 隐藏及呼出、隐藏中等待一分钟 | 按用户选择继续实时运行，呼出正常 |
| 挂靠应用从系统桌面打开 | 全屏画面不被错误退后台 |
| 挂靠时锁屏、关闭插件总开关 | 小窗和保护正确清理，解锁无残留阻断 |
| 完全关闭小窗后再正常操作应用 | 无触摸被错误吞掉；没有卡片遗留 |
| 改圆角与卡片宽度 | 设置立即生效，缓存没有挡住设置更新 |

先在诊断关闭的情况下测试操作；出现问题再短时开诊断复现。测试失败应保留失败步骤和日志，回退原始 0.9.57 包，不把语法检查当成设备兼容性保证。

## 同条件 A/B 发热比较

1. 原版与修复版用同一设备、同样亮度/刷新率设置、相同网络、同一前台应用和相同小窗页面；每轮从近似温度开始，避免把充电或刚启动应用的工作混入对比。正常耗电对比关闭诊断。
2. 分别测：插件关闭且无小窗；插件启用但无小窗；静止居中；静止挂靠；边缘隐藏；关闭小窗后的静置。每种状态至少观察约 5 分钟，重复一轮交换版本顺序。不要只比较摸起来热不热。
3. 如果有可用设备分析工具，记录 SpringBoard 和被托管应用各自的 CPU 时间/占用，同时看渲染、视频解码和网络活动。系统热状态只能辅助判断，不是精确摄氏温度或耗电瓦数。
4. 如果原版和修复版都只在实时小窗/隐藏时热，而没有小窗时正常，应重点检查被托管应用的前台工作与实时保活成本。如果关闭后仍持续异常，则检查实际 Scene 后台状态、其他持有者以及其他插件。

## 新增 energy-sample 的解释

仅在诊断开启、原活动小窗监控运行时，每约五秒产生一条；没有新增常驻计时器。

- cpuPct：SpringBoard 进程在 interval 秒内的 CPU 平均占用，单个 CPU 核满负载为 100%；包括同进程其他插件，不是 Flyme 独占消耗。
- thermal：NSProcessInfo 提供的系统热状态枚举，不是摄氏温度。
- protected：当前是否仍保护场景；隐藏实时模式下为 1 属于预期。
- inputLinkActive / highRefreshActive：静止且动画结束后应为 0；真正拖动或动画期间可以为 1。

这些指标不包含微信 CPU/GPU，也不能覆盖小窗完全关闭后停止的监控周期。第一轮采样先建立基线；跨越空闲周期时要结合 interval 看待 CPU 平均值。

日志采用短时批量写入。停止捕捉后等待约一秒再导出，避免分享快照漏掉尚未落盘的尾部；进程崩溃仍可能丢失最后一批缓冲日志。diagnostic-dropped 表示捕捉拥塞时主动丢弃了部分本地日志。

## 验证结果的边界

已验证：源码可在目标 SDK 下通过语法检查，关键纯逻辑经编译后可执行，静态保护条件存在，源包完整。

未验证：设备上的私有 selector 兼容性、加载器/签名接受情况、实际 Scene 退后台结果、跨进程时序、真实触摸手感和整机功耗。没有宣称修复版已经彻底消除发热。
