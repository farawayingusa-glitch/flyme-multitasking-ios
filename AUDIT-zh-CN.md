# Flyme Multitasking 0.9.57 发热审计与 0.9.58 修复说明

日期：2026-09-11。交付性质：源码修复候选，已做编译语法和可执行逻辑检查，未做手机温度/功耗实测。

## 结论

不是只关日志就能解决的问题。原源码同时存在重复文件读写/跨进程通知、非目标进程中的额外工作、空闲高刷回调、圆角模块异常情况下的无限重试，以及退后台时撤销保活顺序不正确的风险。已修复这些可定位的额外开销。

但这份短日志没有 CPU/GPU、能耗或温度采样，不能证明某一个缺陷就是此次发热的唯一或最大来源，也不能据此给出降温百分比。

按用户明确选择，边缘隐藏后保留实时运行。原机制会把被托管 Scene 保持为 foreground=YES、backgrounded=NO；卡片缩小或移出屏幕不等于应用挂起。该机制的基础耗电依然保留。若应用本身持续动画、视频解码、网络或计算，插件无法在保留这些实时行为的同时让其达到普通后台挂起的耗电水平。要进一步消除这部分，需要改成隐藏暂停/快照后恢复，本次没有这样改。

## 日志能证明什么

输入日志跨度 36.525 秒，145,122 字节，共 930 行；主要是连续六次打开/关闭微信居中小窗。

| 指标 | 数量 | 解读 |
|---|---:|---|
| 居中打开 / 关闭 | 6 / 6 | 短时间交互样本，不是长期待机样本 |
| shared-state publish / write | 66 / 66 | 原代码逐次序列化并原子替换文件 |
| 连续 publish 显示字段相同 | 32 | 指日志打印的字段相同；不把它误当作完整 plist 比较结果 |
| route-publish | 36 | 相同会话的路由在准备过程中多次发布 |
| route-reload | 204 | 多个进程接收通知、读取共享状态 |
| adapter-handshake accepted=0 | 54 | 本样本没有取得有效的目标适配器握手证据 |
| Dock display link 启动记录 | 0 | 本日志不能证明 Dock 高刷泄漏发生过 |
| CPU / GPU / 温度采样 | 0 | 无法据此定量归因 |

同一秒最多 71 行。notify 诊断通道传送的是共享的最后状态，可能合并或重复读取，不能把 204 条记录等同于 204 次独立、准确完整的执行追踪，也不能拿日志行数直接换算 CPU 占用。

## 逐项发现及修复

### 1. 状态写盘和通知重复：日志与源码均支持

原 Tweak.xm 的 FLMScheduleKeyboardSharedStateWrite 每次调用都生成带 updatedAt 的字典，并排一个序列化/原子写盘任务。路由、避让和几何更新各自调用；路由还分别发布三个通知。Keyboard.xm 收到后重载路由，再分别读取几何、避让，同一轮可重复读三次文件。

修复：比较全部有效状态字段，updatedAt 不参与相等判断；用一个受锁保护的“最新待写快照”和一个待执行任务合并约 16ms 内的变化；成功写过的相同最终快照不再写；失败使缓存失效，后续事件可以重试。旧通知状态也做成功后的去重，set/post 任一步失败都不能留下有效的旧缓存。三个路由状态仍先设置再通知。关闭键盘的目标/会话专用请求仍立即发布，不等待合并写盘。

影响：共享文件的发布合并窗口约 16ms，实际执行仍受调度影响。没有删掉状态通道，也没有改变文件字段含义。实机必须检查快速关闭重开与键盘首次出现。

### 2. 每个触摸事件都调用 notify_get_state：源码确定存在

原 Keyboard.xm 的 UIApplication sendEvent: 钩子安装在普通 UIKit 应用进程，触摸事件都会读取 Dock 阻断状态，即使该应用并非当前小窗目标。notify_register_check 并不意味着之后的 notify_get_state 也只是读取共享内存；Apple 的 libnotify 实现包含服务器状态查询路径。[Apple libnotify 源码](https://github.com/apple-oss-distributions/Libnotify/blob/main/notify_client.c)

修复：用 notify_check 的变化标志判断何时需要刷新缓存；第一次调用、状态变化或检查失败才获取状态。保留同步目标匹配及被阻断触摸尾部追踪。实际提取的 C 函数已测试目标隔离、状态切换、读取失败恢复及 20,000 次不变读取。

边界：UIKit 广泛注入和轻量事件入口仍存在，这与支持任意轮盘应用有关；本次没有缩小为只支持微信，也没有通过降低触摸隔离能力来省电。实际触摸所有权和通知跨进程到达时序仍需手机验证。

### 3. 键盘查询重复重载和非目标应用重试：源码确定存在

原 keyboardScreenReferenceSize 与 intersectionHeightForWindowScene 每次进入都调用路由重载；旧路由重载会重复读取共享文件并触发下游工作。非目标但身份已明确的应用还会执行最多八次初始化重试，等待一个并未选择自己的路由。

修复：共享状态按通知变更缓存，按快照修订号避免重复应用；沙盒无法读取 plist 时，旧 notify 退路也使用变化标志，减少不变几何查询中的服务器读取。身份正常的非目标应用依靠已注册路由观察者等待选择；仅观察者注册失败时保留有界重试。注册成功标志改为核对每个实际 token，避免把失败注册误当成功。

### 4. 撤销场景保活晚于退后台请求：源码风险，日志未证明实际残留

原 Tweak.xm:7570 的 backgroundFloatingScene 先执行 updateSettings，再在函数末尾 FLMClearProtectedScene。SceneLifecycle.xm 的钩子会把被保护的 Scene 的 deactivationReasons 改回 0，并强制 foreground=YES/backgrounded=NO。若该后台事务经过这些钩子，就可能被自己的保活逻辑改回前台。

修复：在请求退后台之前先撤销对应 Scene 保护。异常路径也不会再因为跳过事务而保留保护。只改变关闭/退后台顺序，隐藏路径保持实时。

这是一个需要排除的关闭后继续耗电风险，不能声称旧日志已经证明微信关闭后一直在前台。私有 API 是否实际接受后台请求、系统是否还有别的 assertion，仍需设备侧验证。

### 5. 圆角模块无限重试：源码确定存在，但不属于本日志已触发证据

原 Radius.xm:238 在找不到 FLMWheelController 时每 0.25 秒递归安排一次重试，没有次数限制。主模块未成功加载、只装圆角模块或发生兼容问题时可以一直运行。

修复：最多 20 次重试、同一时间只允许一个待重试；没有主控制器时不安装全局 CALayer 钩子。设置变化仍可再次尝试发现控制器。圆角和居中卡片宽度改为设置更新时缓存，动画中不再重复读偏好。

当前日志已经出现主控制器功能，不能把这个异常场景直接当作此次发热主因。

### 6. 高刷与定时器：保留正常跟手，去掉空转

原输入 display link 在触摸开始后到结束前一直活跃；没有待处理位置时仅 return，仍然可以不断回调。原动画 lease 本身有延迟释放，因此并非已经证实的无限高刷。

修复：输入帧被处理或没有待处理输入时暂停；新位置到达同步恢复，终止事件仍先提交最后一点再释放。动画 lease 使用到期时间，避免每次续期新增延迟任务；期限取当前和新动画中较晚者；单次传入时长限定为 0.08–2.0 秒，另有 0.12 秒收尾，在到期回调释放。低电量/严重热状态不额外创建动画高刷 lease，输入刷新请求上限收敛到 60。正常状态仍请求设备高刷，实际帧率由系统决定。[Apple CADisplayLink](https://developer.apple.com/documentation/QuartzCore/CADisplayLink)

锁屏/外部激活检测仍保留原 0.35 秒活动小窗计时器，增加 0.05 秒 tolerance。保留它是为检测锁屏及用户从系统打开已挂靠应用，不宣称这部分监测成本归零。[Apple 定时器能效说明](https://developer.apple.com/library/archive/documentation/Performance/Conceptual/EnergyGuide-iOS/MinimizeTimerUse.html)

### 7. 其他重复工作

- 场景准备完成后，后续 presenter 轮询不再每次重复 activate / updateSettings；场景变化、首次准备或保护丢失时仍重做准备。
- 未处于有效小窗会话的键盘宿主回调在入队前返回。
- 不再因为相同宿主回调无条件 setNeedsLayout；真实 UIKit 脏布局仍由 layoutIfNeeded 处理。
- 删除全屏交互中第二份始终 alpha=0 的快照；保留可见快照及其手势动画。
- 日志改为约 100ms 分批写入，缓冲约 64KB，待处理本地日志最多 512 条；拥塞时报告丢弃计数。关闭捕捉后完成已缓冲尾部的一次写入，不继续采集。
- 原 centered-content-ready 日志漏传物理卡片高度，导致后续浮点值错位、最后的整数出现乱码。补齐参数，并添加 NS_FORMAT_FUNCTION 与编译期格式错误检查。日志里的这个乱码不能作为运行时内存损坏证据。
- 仅用于诊断的握手查询在捕捉关闭时跳过，不再做无功能收益的状态查询。

## 已检查但未判为本次热源

- 主模块初始化使用 dispatch_once，没有发现 start 重复注册观察者。
- 主场景启动有 6.5 秒总超时和次数上限；presenter 失败有回收/回退；全屏交接轮询最多 24 次；显示遮罩恢复最多四次。不是无限循环。
- 旧应用身份重试虽多余，但原本有八次上限，与圆角模块无限重试性质不同。
- 全屏截图是手势开始时生成；没有发现整段代码中按每帧重新截图的循环。
- 设置页的应用枚举、写偏好、分享日志由用户操作触发，未发现常驻采样计时器。
- 当前源码的旧 content viewport 布局适配应使用 shouldApply=NO，未重新启用它。
- 原注入范围、键盘精确 Scene 匹配、rootless 配置、签名工具选择、手势阈值、裁切尺寸及隐藏实时模式保留。

## 握手失败和机制边界

日志中的 54 次 accepted=0 表示没有读到有效 ctor/ready 证据，不能单凭这一点断定“插件完全未注入微信”，更不能据此归因为热源。目标适配器是否载入、签名/越狱加载器是否允许、实际运行包是否就是这份源码，都需要真实设备与构建包证据。原日志没有启动版本行，源码身份也无法靠这份短片段完全认证。

日志的 scene-pair apply=unsupported 是私有接口兼容证据，样本随后仍出现 native host paired。不能简单等同于键盘失效或循环报错。未在本次修改中强行调用系统不支持的 selector，也没有改为绕过签名或保护机制。

当前保留的实时托管、广泛 UIKit 注入和活动小窗锁屏监测都有基础成本。修复额外工作不等于承诺系统层面零开销。

## 后续怎么确认真的改善

先按 VALIDATION.md 的同条件 A/B 步骤测原版与修复版。普通对比关闭诊断；排障只短期开诊断。新版 energy-sample 每约五秒输出 SpringBoard 进程 CPU、热状态、场景保护与 display link 状态。CPU 是进程总值，包含 SpringBoard 中其他插件，不是 Flyme 独占 CPU；更不包含微信和 GPU。

静止挂靠/隐藏、未触摸、动画结束后，inputLinkActive 和 highRefreshActive 应均为 0。hidden=1 时 protected=1 在本次保留实时策略下是预期现象。关闭后仍明显发热，需要分别检查目标应用、SpringBoard、渲染/视频/网络等实际活动，不能继续凭短功能日志猜测。

最终结论：额外开销的源码修复已交付；实际发热的主导来源和最终降温效果尚未实测确认。隐藏实时运行的基础耗电是保留的机制限制。
